# 카테고리 지도 (학습 줄기)

학습 로그를 9개 카테고리로 묶고, 각 카테고리의 **목적**과 **저울(트레이드오프)**로 한 줄에 꿴다.
*(2026-06-15 정리 … 2026-07-07 갱신[#58·59 재시도 정책 arc 닫힘], 2026-07-08 재편[종합-인출 1부: DB·트랜잭션 재봉인, **외부 API 장애 대응** 분리 신설], **2026-07-09 완료**[종합-인출 2부(종합_2부): 6칸(테스트·인증·모델링·아키텍처·Spring웹·학습법) 재봉인 — **9칸 전부 학습자 인출로 봉인**, Spring웹 미탐구 탈출. 회고 정리물은 [`Level2_정리/`](Level2_정리/) 폴더의 카테고리별 파일 + 스킬 스냅샷] — 로그를 직접 인출해 한 줄로 꿴 결과. 개별 로그는 [인덱스](README.md) 참고.)*

## 한눈에

| 카테고리 | 목적 | 저울 (트레이드오프) | 대표 로그 |
|---|---|---|---|
| 🗄️ **DB·트랜잭션** | 동시성 속의 정확성 (ACID) | 격리수준 vs 성능 · 낙관 vs 비관(경합 드물면 낙관, 재시도 비용·순서 정합성이 중요하면 비관) · 원리: 값을 가져와서 비교하는 로직에는 틈이 있다 — 일관성은 UNIQUE 제약(탄생) 또는 CAS(전이)가 보장하고, 애플리케이션 로직은 의도 표현과 친절한 거절을 맡는다(역할 분담, 대체 불가) | 30·31 (+03·05·06·08·12·13·17·26·29·47·55~58) |
| 🔑 **인증·세션** | HTTP는 무상태라, 클라이언트는 매 요청마다 신원을 증명할 증표를 전달해야 한다 (세션=JSESSIONID / 토큰=JWT) | 상태를 서버에 저장하면 무효화가 편하고, 안 하면 확장이 편하다 · 절충: 짧은 JWT(빈번한 검증 길) + refresh token(드문 재발급 길 — 서버 저장이라 탈취돼도 지워서 죽일 수 있다) | 32 (+16·18·19·22) |
| 🧩 **복잡한 모델링** | 책임을 그 데이터를 가진 객체에 묶어 변경·확장을 쉽게 한다 (정보 전문가) — 흩어지면 같은 로직이 Service 여기저기 퍼져 수정이 어려워진다 | ① 묶기 vs 나누기: 데이터·메서드·인스턴스가 같아도 의미(역할)가 다르면 안 합친다 (억지 결합은 변경을 더 어렵게 — Reservation의 member ≠ Waiting의 member) · ② 도메인 vs Service: 자기가 든 데이터만으로 판단되고 크기가 작으면 도메인(Waitings ≤5), 남의 테이블 조회가 필요하거나 크면 Service · ③ 다리: 규칙이 도메인에 못 들어가 Service로 밀리는 그 지점이 곧 DB 칸의 출발점(조회→INSERT race) — Unique가 진실을 받치고 앱은 의도 표현(1부 역할 분담) | 33 (+23·24·25·14) |
| 🧪 **테스트** | 변경을 가능하게 한다 — 기능 명세서처럼 각 층의 책임을 명시하기 때문 | ① 격리 ↔ 실제성(검증 대상이 "진짜"를 요구하는 만큼만 무겁게) · ② 테스트 단위는 "어떤 책임을 검증하나"가 나눈다: Domain=외부 경계 없어 단위 / Service=DB·트랜잭션·락 엮여 통합 / Controller=API 계약(슬라이스 @WebMvcTest) or 사용자 흐름(인수) | 34·61 (+02·27·56) |
| 💳 **외부 결제 연동** | 클라이언트도 외부도 믿지 않는다 — 돈의 정확성은 서버가 쥔다 | 코어: 인증 vs 승인 · 금액 검증(서버 저장값과 대조) · 주문 상태 기계(확정 안 된 상태 포함) | 35·37·43·45 (+53 멱등키 반쪽) |
| 🛡️ **외부 API 장애 대응** 🆕 | 네트워크 장애, 서버 다운과 같은 환경의 장애가 우리의 장애로 전파되지 않도록 설계한다 | 실패의 생애주기 3장 (아래 상세) — 결제 시스템이라는 무대에서 경험한 일반 원리들 | 36·44·46·48·49·50·52·53·54·58·59 |
| 🏗️ **아키텍처·의존성** | 무엇이 무엇을 알게 할지(의존 방향)와 경계를 정한다 — 변경이 한 곳에 갇혀 번지지 않도록 | 순환참조 끊기는 "즉시 필요한가"로 갈린다(즉시=중립 담당자 Order로 한 방향 / 나중=메모+워커=아웃박스) · 묶기 vs 떼기 = 강한 일관성 ↔ 최종 일관성(A의 취소가 B 승격 장애로 불발 안 되게 뗌, 대가는 멱등·동시성·빈 창 예외) · ACL(외부 타입은 어댑터 안에 가둔다) · 가드(import 자동 검사 ArchUnit) | 42 (+38·40·41 / 운영: 39) |
| 🌱 **Spring 웹 흐름** | 요청–응답 파이프라인에 횡단 관심사(인증 등)를 어디 끼우나 — 위치가 능력을 정한다 | Filter(컨테이너 앞단, forward 커버) vs Interceptor(DispatcherServlet 내부, forward 못 감) → 인증은 Filter로 · 인증=요청당 한 번(OncePerRequestFilter: 신원은 forward에도 안 바뀌니 "한 번"이 옳다) · Filter는 앞단이라 @ExceptionHandler 밖 → 에러 직접 직렬화 · 역직렬화는 ArgumentResolver 단계(타입 안 맞으면 @Valid 전에 실패) | 01·10·11·15·21 |
| 🌀 **학습법** | 재미(주도권)를 위해 정답을 안 주고 스스로 찾게 → 재방문으로 이해 쌓기 | 깊이 ↔ 폭·반복(나선형은 폭을 택하고 깊이는 재방문으로 축적) · 엔진=재방문(한 곳에 모아 깊이만 더함) · 방법론이 스스로 학습(매 사이클 "바꿀 것") | 전체 · [BACKLOG](BACKLOG.md) |

## 🛡️ 외부 API 장애 대응 — 실패의 생애주기 3장

> **목적**: 네트워크 장애, 서버 다운과 같은 환경의 장애가 우리의 장애로 전파되지 않도록 설계한다.

1. **모름과 수렴** (실패가 진행 중일 때) — 응답을 못 받은 요청은 "실패"가 아니라 **"모름"**이다. 멱등키가 재시도를 안전하게 만들고, 조회를 통해 확정 상태(성공 or 실패)로 수렴시킨다.
2. **실패의 결말** (실패가 확정될 때) — 분리된 세 장치:
   - **재시도 한도**: 일시적 실패는 재시도하되 한도를 둔다. 한도를 넘었다는 건 일시적이 아니었다는 뜻.
   - **DEAD 격리**: 한도를 넘은 건은 버리지 않고 자동 처리에서 빼내 사람이 처리한다.
   - **보상**: 롤백할 수 없는 외부 행동은 반대 행동으로 되돌린다 (예: 환불).
3. **방어벽** (실패가 번지기 전에) — Rate Limit은 호출량을 지키고, Circuit Breaker는 죽은 상대를 계속 때리지 않게 지킨다. 외부 환경 문제로 인한 에러는 개별 건의 실패 횟수로 세지 않는다.

## 메타 패턴 — "저울"

DB·인증·모델링·테스트 네 카테고리가 같은 모양을 보였다:

> **양쪽을 동시에 못 가지니, 상황에 맞게 저울추를 놓는다.**

- **정확성 ↔ 동시성**: 락을 섞어 쓴다 (낙관 + 비관). 단 가드(잠금 검사)는 빈 범위에선 gap 락이라 입장을 직렬화 못 한다 — 진실은 쓰기 지점의 원자 장치(CAS·UNIQUE)가 가린다 (#47·56).
- **확장성 ↔ 무효화**: 세션·JWT를 섞어 쓴다 (짧은 JWT + refresh token).
- **묶기 ↔ 나누기**: 데이터가 같아도 *역할*이 다르면 나눈다.
- **격리 ↔ 실제성**: 검증 대상이 "진짜"를 요구하는 만큼만 무겁게 간다. 진짜 경합·락 사고(데드락)는 진짜 엔진 위에서만 보인다 (#56 Testcontainers).
- **중복 막기 ↔ 멱등 재시도**: 한 겹으로 안 되니 겹쳐 막는다 (getOrCreate + 멱등 취소 + UNIQUE 백스톱).
- **스레드 보호 ↔ 모름·재시도**: 타임아웃을 짧게 할수록 원래 성공할 호출까지 끊겨 '모름'·재시도가 늘어 멱등키가 더 중요해진다 (log_44~45).
- **버스트 ↔ 평균 처리량**: 토큰 버킷의 capacity(순간 버스트)와 refill(평균 상한)을 따로 잡고, 인바운드(내 처리 용량)와 아웃바운드(상대가 허용한 몫)를 다른 값으로 둔다 (log_49).
- **fail-fast ↔ 블로킹 대기**: 대기시간 × 동시 대기자 수로 고른다 — 무계 손님은 끊고(429+Retry-After), 유계 워커는 붙잡되 마감을 건다(bounded wait, 넘기면 거절 폴백) (log_52).
- **양 ↔ 건강**: Rate Limit은 양을 재서 평시 quota 약속을 지키고, 서킷 브레이커는 건강(실패율 AND 최소 표본)을 재서 장애 시 스레드를 지킨다 — 재는 축이 달라 서로를 대신 못 하니 둘 다 (log_54).

## 트랜잭션(ACID)을 축으로 한 줄기

트랜잭션이 여러 카테고리를 꿴다:

- **격리성(I)** → 동시성·락 (DB·트랜잭션)
- **원자성(A)** → 트랜잭션 경계·아웃박스 → Saga·멱등성 (31)
- **일관성(C, 비즈니스 규칙)** → 도메인 모델링 (33: *행 간 불변식*이 도메인↔협력 경계를 가른다)
- **경계가 진짜 지켜지나** → 테스트 (34: *테스트가 트랜잭션을 쥐면 경계가 가려진다*)
- 그리고 **unique 제약**이 INSERT race · 멱등성 · 백스톱으로 반복 등장 (30·31·**43**: 결제 주문 중복까지 같은 도구로).

## 종합-인출 완료 (2026-07-09)

- **9칸 전부 학습자 인출로 재봉인 완료** — 1부(종합_1부, 2026-07-08): DB·트랜잭션·결제·외부 API 장애 대응 / 2부(종합_2부, 2026-07-09): 테스트·인증·세션·복잡한 모델링·아키텍처·의존성·Spring 웹 흐름·학습법.
- **회고 정리물**: [`Level2_정리/`](Level2_정리/) — 카테고리별 파일 9개(표 한 줄이 아니라 풀어쓴 전사) + [나선형학습 스킬 스냅샷](Level2_정리/나선형학습-스킬스냅샷.md).
- 카테고리별 다음 키워드(i+1)는 [BACKLOG.md](BACKLOG.md).
